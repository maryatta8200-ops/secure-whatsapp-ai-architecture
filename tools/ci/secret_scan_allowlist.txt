# Paths excluded from tools/ci/secret_scan.py.
#
# Only files that provably contain non-secret fixtures belong here. Every entry
# is reviewed as part of the milestone review, because an entry here disables
# credential detection for that path.
#
# Unit test fixtures: the values are labelled non-secret and are only ever used
# to exercise redaction, signature validation and configuration parsing.
core/src/test/**
data/src/test/**
app/src/test/**
#
# Local sandbox test shim: reproduces a subset of the JUnit API so the domain
# tests can run offline. Contains no credentials.
tools/local-verify/**
#
# This file documents itself and matches the "assigned-secret" pattern.
tools/ci/secret_scan_allowlist.txt
#
# Documentation describing which environment variables hold real secrets. The
# variable names are not secrets.
docs/SECURITY.md
